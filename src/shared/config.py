import os

base_drive = "E:/"

base_folder = os.path.join(base_drive, "TRADER_1_DATA")

source_of_truth_data_folder = os.path.join(base_folder, "source_of_truth_data")

data_folder = os.path.join(base_folder, "data")